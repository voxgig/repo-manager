{
  "name": "repo-manager-web",
  "version": "0.1.0",
  "private": true,
  "description": "RepoManager SPA: web components on a Seneca service bus",
  "scripts": {
    "postinstall": "node -e \"const fs=require('fs');fs.mkdirSync('public',{recursive:true});fs.copyFileSync(require.resolve('seneca-browser'),'public/seneca-browser.js')\"",
    "dev": "vite",
    "build": "vite build",
    "e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "seneca-browser": "^8.0.0-rc4",
    "vite": "^7.3.4"
  }
}
